// core/config
// Central place for environment-driven Firebase settings (mocked in this sample).

export const firebaseConfig = {
  projectId: "teh-tarik-app-my-own",
  apiKey: "mock-api-key",
  appId: "mock-app-id",
};
