// core/config
// Central place for environment-driven Firebase settings (mocked in this sample).

export const firebaseConfig = {
  projectId: "teh-tarik-dev",
  apiKey: "mock-api-key",
  appId: "mock-app-id",
};
