/**
 * Metro resolves `.web` / `.native` — TypeScript default follows `.web` (see stripeService pattern).
 */
export type {
  ProductOptionsSheetProduct,
  ProductOptionsPayload,
  ProductOptionsSheetRef,
  ProductOptionsBottomSheetProps,
} from './ProductOptionsBottomSheet.types';
export { ProductOptionsBottomSheet } from './ProductOptionsBottomSheet.web';
