/**
 * @file Metro entry — keep logic in `src/bootstrap/loadApp.ts`.
 * Do not add dynamic requires with computed paths; Metro requires string literals.
 */
import { bootstrap } from "./src/bootstrap/loadApp";

bootstrap();
