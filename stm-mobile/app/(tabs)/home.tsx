/**
 * Tab “Home” — full STM home (featured menu, gallery, tracking, support).
 * `HomeTabScreen` lives only under delivery experiments; the customer tab uses `HomeScreen`.
 */
export { default } from '@/src/screens/HomeScreen';
