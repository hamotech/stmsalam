/**
 * Route: /payment-method — Grab-style payment method selection (after checkout draft).
 */

import PaymentMethodScreen from '@/src/screens/PaymentMethodScreen';

export default PaymentMethodScreen;
